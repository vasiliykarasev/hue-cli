import os
from setuptools import setup

setup(
    name = "hue executor",
    version = "0.0.4",
    author_email = "karasev00@gmail.com",
    description = ("Hue CLI"),
    license = "GPL",
    scripts = ['hue'],
)
